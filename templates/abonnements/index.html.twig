{% extends 'base.html.twig' %}

{% block title %}Abonnements index{% endblock %}

{% block body %}
    <h1>Abonnements index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>News</th>
                <th>Evenements</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonnement in abonnements %}
            <tr>
                <td>{{ abonnement.id }}</td>
                <td>{{ abonnement.News }}</td>
                <td>{{ abonnement.Evenements }}</td>
                <td>
                    <a href="{{ path('abonnements_show', {'id': abonnement.id}) }}">show</a>
                    <a href="{{ path('abonnements_edit', {'id': abonnement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('abonnements_new') }}">Create new</a>
{% endblock %}
